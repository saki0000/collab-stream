# 機能仕様: {Feature Name}

> Story: {Story N} of {EPIC-XXX} | Issue: #{N}

---

## 1. ユーザーストーリー

- {ユーザーが〇〇すると、△△する}
- {例：ユーザーが画面を開くと、データが自動的に読み込まれる}
- {例：読み込み中はローディングを表示する}

---

## 2. ビジネスルール

| ドメイン | ルール | 条件/値 | US |
|----------|--------|---------|-----|
| {ドメイン名} | {ルール名} | {条件または値} | {US番号} |
| 例: カレンダー | 表示範囲 | 過去7日〜当日 | 1 |
| 例: カレンダー | デフォルト | 今日 | 1 |
| 例: チャンネル | 最大数 | 10 | 1,2 |
| 例: バー表示 | 開始位置 | 選択日より前なら0:00から | 1 |
| 例: 同期状態 | WAITING判定 | 同期時刻がストリーム開始前 | 3 |

---

## 3. 状態遷移

```mermaid
stateDiagram-v2
    [*] --> 読み込み中

    読み込み中 --> コンテンツ表示: 成功（データあり）
    読み込み中 --> 空表示: 成功（データなし）
    読み込み中 --> エラー表示: 失敗

    コンテンツ表示 --> 読み込み中: 更新
    エラー表示 --> 読み込み中: 再試行
    空表示 --> 読み込み中: 更新
    空表示 --> コンテンツ表示: データ追加
```
